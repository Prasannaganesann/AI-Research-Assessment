"""
utils/
======
Shared utility modules used across the entire project.

Modules:
    - logger : Structlog configuration (JSON or console renderer)
    - retry  : Tenacity-based retry decorator with exponential backoff
"""
